import AccountSettings from "./AccountSettings";

const SuperAdminSettings = () => {
  return <AccountSettings returnPath="/admin/dashboard" themeColor="purple" />;
};

export default SuperAdminSettings;
